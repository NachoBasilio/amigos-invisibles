---
import asignaciones from "../data/amigos.json";
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>🎁 Amigo Invisible</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- SEO básico -->
    <meta name="description" content="Ingresá a tu enlace único del Amigo Invisible 🎁. No seas chismoso 😏" />

    <!-- Open Graph / Social -->
    <meta property="og:title" content="🎁 Amigo Invisible" />
    <meta property="og:description" content="Ingresá al enlace único que te enviaron para ver tu regalo 😉" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://amigos-invisibles.netlify.app/" />
    <meta property="og:image" content="https://emojiapi.dev/api/v1/gift/512.png" />

    <!-- Favicon con emoji (sin imagen real) -->
    <link rel="icon" href="data:image/svg+xml,
      %3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E
        %3Ctext y='0.9em' font-size='90'%3E🎁%3C/text%3E
      %3C/svg%3E" />

    <style>
      :root {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        color-scheme: dark;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        text-align: center;
        background: radial-gradient(circle at top, #ffb6c1 0, #120418 45%, #050308 100%);
        color: #fff;
      }

      .card {
        width: 100%;
        max-width: 480px;
        background: rgba(15, 10, 20, 0.85);
        border-radius: 1.5rem;
        padding: 2rem 1.8rem 2.3rem;
        box-shadow: 0 18px 45px rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(12px);
      }

      h1 {
        font-size: clamp(1.8rem, 6vw, 2.3rem);
        margin: 0 0 1rem;
      }

      p {
        font-size: clamp(1rem, 3.5vw, 1.15rem);
        opacity: 0.9;
        line-height: 1.5;
        margin: 0.7rem 0;
      }
    </style>
  </head>

  <body>
    <div class="card">
      <h1>Amigo Invisible 🎁</h1>
      <p>Esta página no tiene contenido público.</p>
      <p>
        Ingresá al enlace único que te enviaron para ver tu regalo.
        <br />
        <strong>CHISMOSO 😏</strong>
      </p>
    </div>
  </body>
</html>
