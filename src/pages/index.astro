---
import asignaciones from "../data/amigos.json";
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Amigo Invisible</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        color-scheme: dark;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        text-align: center;

        /* mismo estilo del [id].astro */
        background: radial-gradient(circle at top, #ffb6c1 0, #120418 45%, #050308 100%);
        color: #fff;
      }

      .card {
        width: 100%;
        max-width: 480px;
        background: rgba(15, 10, 20, 0.85);
        border-radius: 1.5rem;
        padding: 2rem 1.8rem 2.3rem;
        box-shadow: 0 18px 45px rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(12px);
      }

      h1 {
        font-size: clamp(1.8rem, 6vw, 2.3rem);
        margin: 0 0 1rem;
      }

      p {
        font-size: clamp(1rem, 3.5vw, 1.15rem);
        opacity: 0.9;
        line-height: 1.5;
        margin: 0.7rem 0;
      }

      @media (min-width: 768px) {
        .card {
          padding: 2.3rem 2.2rem 2.6rem;
        }
      }
    </style>
  </head>

  <body>
    <div class="card">
      <h1>Amigo Invisible 🎁</h1>
      <p>Esta página no tiene contenido público.</p>
      <p>
        Ingresá al enlace único que te enviaron para ver tu regalo.
        <br />
        <strong>CHISMOSO 😏</strong>
      </p>
    </div>
  </body>
</html>
